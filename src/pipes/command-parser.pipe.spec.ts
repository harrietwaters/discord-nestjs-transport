import { CommandParserPipe } from './command-parser.pipe';

describe('CommandParserPipe', () => {
    it('should be defined', () => {
        expect(new CommandParserPipe()).toBeDefined();
    });
});
